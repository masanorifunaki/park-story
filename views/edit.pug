extends layout

block content
  form(method="post", action=`/course/${course.courseId}?edit=1`)
    input(type="hidden" name="_csrf" value!=csrfToken)
    div
      h5 コース名
      input(type="text" name="courseName")
    div
      h5 メモ
      textarea(name="courseMemo")
    div
      h5 候補日
      input(type="date", name="courseDay")
      p 候補時間の追加 (改行して複数入力してください)
      textarea(name="candidates")
    div
      button(type="submit")  以上の内容で編集する
  div
    p 既存の候補時間
    each candidate in candidates
      button(data-candidate-id=candidate.candidateId data-course-id=candidate.courseId).deleteCandidateAppointment #{candidate.candidateTime} 削除する