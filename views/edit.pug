extends layout

block content
  form(action="/courses", method="post")
    div
      h5 コース名
      input(type="text" name="courseName")
    div
      h5 詳細
      textarea(name="courseDescription")
    div
      h5 日程
      input(type="datetime-local", name="courseTime")
    button(type="submit") コースを作る