extends layout

block content
  div.page
    div.page-container
      h2 あなたが参加予定のコースが表示されます。
      if candidates
        each c in candidates
          h3.headline コース名：#{c.candidate.course.courseName}
          p 日付：#{c.course.formattedCourseDay}
          p 時間：#{c.candidate.candidateTime}
          p 場所：#{c.course.coursePlace}
          a(href=`/course/${c.candidate.course.courseId}/${c.candidate.candidateId}`).btn 確認する

