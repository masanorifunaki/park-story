doctype html
html
  head
    title 東京公園ストーリー
    meta(charset="utf-8")
    meta(http-equiv="X-UA-Compatible" content="IE=edge")
    meta(name="viewport" content="width=device-width, initial-scale=1")
  body
    form(method="post", action="/course" enctype="multipart/form-data")
      input(type="hidden" name="_csrf" value!=csrfToken)
      div
        h5 コース名
        input(type="text" name="courseName")
      div
        h5 メモ
        textarea(name="courseMemo")
      div
        h5 候補日
        input(type="date", name="courseDay")
      div
        h5 画像
        input(type="file", name="courseImgFile")
      div
        h5 候補時間 改行して入力してください
        textarea(name="candidates")
      button(type="submit") コースをつくる。
    script(src="/javascripts/bundle.js")