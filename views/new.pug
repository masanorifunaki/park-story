extends layout

block content
  form(method="post", action="/course")
    div
      h5 コース名
      input(type="text" name="courseName")
    div
      h5 メモ
      textarea(name="mecourseMemo")
    div
      h5 候補日
      input(type="date", name="courseDay")
    div
      h5 候補時間
      input(type="datetime", name="candidateTime")
    button(type="submit") コースをつくる。